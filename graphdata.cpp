#include "GraphData.h"

GraphData::GraphData(QObject* parent) : QObject(parent) {

}

// void GraphData::updateData(int index, const QVector<QPair<int, float>>& newData) {
//     if (index >= 0 && index < 7) {
//         data[index] = newData;
//     }
// }

